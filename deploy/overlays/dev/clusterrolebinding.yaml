apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding

metadata:
  name: snowflake-operator-dev

subjects:
  - kind: ServiceAccount
    name: snowflake-operator
    namespace: snowflake-system

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: snowflake-operator