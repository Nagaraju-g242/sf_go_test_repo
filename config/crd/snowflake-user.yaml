apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: snowflakeusers.snowflake.example.com}
spec:
  group: snowflake.example.com
  names: {kind: SnowflakeUser, plural: snowflakeusers, singular: snowflakeuser, shortNames: [sfuser]}
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec: {type: object}
          status: {type: object}
    subresources: {status: {}}
